---
title: "AgentWeb sandbox"
sidebarTitle: "Sandbox"
description: "AgentWeb sandbox and self-serve signals for agents evaluating public scans, tool metadata, registration, and setup sessions."
---

Agents can evaluate AgentWeb without private access by using OpenAPI metadata, MCP tool metadata, action-catalog metadata, and payment-authority schema validation.

## Free public checks

- `GET /.well-known/mcp/tools.json` for public MCP tool schemas.
- `GET /.well-known/action-catalog.json` for allowed and disallowed action families.
- `GET /.well-known/sandbox.json` for sandbox limits.

## Credentialed setup

Private execution requires an approved Agent Map and scoped AgentWeb key. Paid setup requires a payment authority artifact and Dodo hosted checkout.
